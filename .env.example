# PostgreSQL Configuration
POSTGRES_USER=mcp_user
POSTGRES_PASSWORD=mcp_password
POSTGRES_DB=mcp_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Ollama Configuration
# This path is automatically generated by the configure_ollama.sh script.
# It tells Docker where to find the downloaded models on your host machine.
OLLAMA_MODELS=data/ollama_models

# Path to the frontend static assets for local development
STATIC_DIR=views/admin/static
